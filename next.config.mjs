/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['example.com', 'images.unsplash.com'], // Correct Unsplash domain
    },
  };
  
  export default nextConfig;
  